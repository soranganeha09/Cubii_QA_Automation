[behave]
paths = features
# Multiple formatters/outfiles must be on separate lines (Behave uses splitlines(); commas do NOT split).
format = pretty
         allure_behave.formatter:AllureFormatter
outfiles = stdout
           allure-results
show_skipped = false
show_timings = true
junit = true
junit_directory = reports
